# Claude will generate this customized for Eagle Sign
def eagle_sign_flywheel():
    # 1. Auto-extract from KeyedIn
    data = keyedin_extractor.get_daily_data()
    
    # 2. Run local variance detection
    variances = detect_anomalies(data)
    
    # 3. If significant variance found
    if variances.severity > threshold:
        alert_user("Upload to Claude for forensic analysis")
    
    # 4. Weekly: Compile and upload for pattern evolution
    if datetime.now().weekday() == 0:  # Monday
        create_weekly_report()