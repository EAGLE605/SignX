# AI/ML Stack for SignX-Studio Cost Intelligence
# Optimized for NVIDIA RTX 5070 Ti (16GB VRAM) with CUDA 12.1
# Python 3.11+ required
# Last updated: 2025-11-02

# ==============================================================================
# INSTALLATION INSTRUCTIONS
# ==============================================================================
# 1. Install RAPIDS via conda first (for cuDF/cuML/cuGraph):
#    conda create -n signx-ml python=3.11 -y
#    conda activate signx-ml
#    conda install -c rapidsai -c conda-forge -c nvidia \
#        rapids=24.10 python=3.11 cuda-version=12.1
#
# 2. Then install these pip requirements:
#    pip install -r requirements-ml.txt
# ==============================================================================

# Core ML frameworks with CUDA 12.1 support
--index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.19.1+cu121
torchaudio==2.4.1+cu121

# Graph Neural Networks (for structural analysis)
torch-geometric==2.5.3
# Install scatter/sparse from PyG wheel index:
# pip install torch-scatter torch-sparse torch-cluster torch-spline-conv \
#     -f https://data.pyg.org/whl/torch-2.4.0+cu121.html

# GPU-accelerated gradient boosting
xgboost==2.1.1
lightgbm==4.5.0

# Traditional ML (works with cuML when using cudf.pandas)
scikit-learn==1.5.2

# PDF extraction and processing
pdfplumber==0.11.4
PyMuPDF==1.24.10
tabula-py==2.9.3
camelot-py[cv]==0.11.0
pdfminer.six==20231228

# Data processing (pairs with cudf.pandas for GPU acceleration)
pandas==2.2.3
numpy==1.26.4
scipy==1.14.1
pyarrow==17.0.0
polars==1.9.0  # Alternative fast dataframe library

# Experiment tracking and model versioning
mlflow==2.16.2
wandb==0.18.3

# Feature engineering
category-encoders==2.6.3
scikit-optimize==0.10.2

# Visualization
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.24.1

# Model serving
fastapi==0.115.0  # Match existing SignX-Studio version
uvicorn==0.31.0
pydantic==2.9.2  # Match existing version

# Utilities
python-dotenv==1.0.1
tqdm==4.66.5
joblib==1.4.2
tenacity==9.0.0
structlog==24.4.0  # Match existing logging

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
pytest-benchmark==4.0.0

# Code quality
ruff==0.7.0  # Match existing linter
mypy==1.12.1
black==24.10.0

# Type stubs for better IDE support
pandas-stubs==2.2.2.240909
types-requests==2.32.0.20240914

